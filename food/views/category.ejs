<%- include("header"); -%>
<h2>Breakfast</h2>
<div class="rows">
    <% for( let index = 0; index < 3; index++ ) { %>
        <div class="row">
            <% for( let i = 0 ; i <4 ; i++ ) { %>
                <div class="dish-row  col-lg-3">
                    <div class="dish-img"><img src="/images/sweetDish/sdr1.jpg" alt=""></div>
                    <div class="dish-name">Dish Name</div>
                </div>
            <% } %>
        </div>
    
    <% } %>
</div>  
<h2>Lunch</h2>
<div class="rows">
    <% for( let index = 0; index < 3; index++ ) { %>
        <div class="row">
            <% for( let i = 0 ; i <4 ; i++ ) { %>
                <div class="dish-row  col-lg-3">
                    <div class="dish-img"><img src="/images/weightLoss/wlr3.jpg" alt=""></div>
                    <div class="dish-name">Dish Name</div>
                </div>
            <% } %>
        </div>
    
    <% } %>
    


</div>
<h2>Dinner</h2>
<div class="rows">
    <% for( let index = 0; index < 3; index++ ) { %>
        <div class="row">
            <% for( let i = 0 ; i <4 ; i++ ) { %>
                <div class="dish-row  col-lg-3">
                    <div class="dish-img"><img src="/images/sweetDish/sdr8.jpg" alt=""></div>
                    <div class="dish-name">Dish Name</div>
                </div>
            <% } %>
        </div>
    
    <% } %>
</div>  


<%- include("footer") -%> 